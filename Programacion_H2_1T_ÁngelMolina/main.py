from menu import menu  # Importo la función menu desde el archivo menu.py

if __name__ == "__main__":  # Compruebo si el script se está ejecutando directamente
    menu()  # Llamo a la función menu para que se ejecute
